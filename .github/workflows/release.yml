name: Release

on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+*"

permissions:
  id-token: write
  contents: read

jobs:
  prebuild:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
        include:
          - platform: linux/amd64
            arch: x64
          - platform: linux/arm64
            arch: arm64
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/setup-qemu-action@v3
        if: matrix.arch == 'arm64'

      - name: Build prebuild (${{ matrix.arch }})
        run: |
          docker buildx build \
            --platform ${{ matrix.platform }} \
            --output type=local,dest=./out \
            -f ./Dockerfile \
            .

      - uses: actions/upload-artifact@v4
        with:
          name: prebuild-linux-${{ matrix.arch }}
          path: out/prebuilds/

  release:
    needs: prebuild
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24.x"
          registry-url: "https://registry.npmjs.org"

      - name: Update npm
        run: npm install -g npm@latest

      - uses: actions/download-artifact@v4
        with:
          pattern: prebuild-*
          path: prebuilds-tmp/

      - name: Merge prebuilds
        run: |
          mkdir -p prebuilds
          cp -r prebuilds-tmp/prebuild-linux-x64/* prebuilds/
          cp -r prebuilds-tmp/prebuild-linux-arm64/* prebuilds/
          echo "Prebuilds:"
          find prebuilds -type f

      - run: npm install --ignore-scripts

      - name: Verify package
        run: node -e "const p = require('./package.json'); console.log(p.name + '@' + p.version)"

      - name: Publish to NPM
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${{ github.ref_name }}" --generate-notes
